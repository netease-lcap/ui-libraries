// import _ from 'lodash';
// // import { ElIcon } from '../../index';

// export function useHandleIcon(props) {
//   const iconProps = props.get('icon');
//   return _.isNil(iconProps) ? {} : { icon: <ElIcon icon={iconProps} /> };
// }
export {};
